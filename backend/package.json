{
  "name": "campusgenie-backend",
  "version": "1.0.0",
  "description": "CampusGenie Backend API",
  "scripts": {
    "start": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "build": "pip install -r requirements.txt"
  },
  "engines": {
    "node": "20.x"
  }
}
