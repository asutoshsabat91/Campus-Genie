{
  "name": "campusgenie-chromadb",
  "version": "1.0.0",
  "description": "CampusGenie ChromaDB Vector Database",
  "scripts": {
    "start": "chroma run --host 0.0.0.0 --port $PORT",
    "build": "pip install chromadb==0.5.3"
  },
  "engines": {
    "node": "20.x"
  }
}
